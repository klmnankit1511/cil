.venv
.browser-profile
raw
__pycache__

# holds a live login token — never commit
.auth.json

# large generated artefacts (rebuilt by build_site_data.py)
downloads/
site/data.js
site/notes.js

# difficulty.json is deliberately NOT ignored: it costs real API spend to
# regenerate, is only a few hundred KB, and the Pages build needs it.

# local credentials — never commit
.env
